Get-ChildItem -Path $ścieżkaDoKatalogu -Recurse -File | Where-Object { $_.DirectoryName -notmatch "\\$wykluczonyFolder\\" } | Select-String -Pattern $szukanyTekst | ForEach-Object {
    Write-Host "Znaleziono tekst w pliku: $($_.Path)"
